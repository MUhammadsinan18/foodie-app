import 'package:flutter/material.dart';
class FoodieColors {
  static const Color primary = Color(0xFFE8602C);   // deep orange
  static const Color accent  = Color(0xFFFFF0E6);   // warm peach bg
  static const Color border  = Color(0xFFFFCBA8);   // peach border
  static const Color text    = Color(0xFF1A1A1A);   // near-black text
  static const Color hint    = Color(0xFFB0825F);   // warm brown hint
  static const Color white   = Color(0xFFFFFFFF);
}